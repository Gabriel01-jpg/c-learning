#include <stdio.h>

int main(){
    int catAge = 2;
    char catName[] = "Yummi";

    // s to array char
    // d to decimal
    // f to floating
    // c to char

    printf("My cat is called %s and have %d years old \n", catName, catAge);

    float half = 0.5;

    printf("The half of one is %f", half);

    return 0;
}
