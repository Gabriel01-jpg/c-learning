#include <stdio.h>

int main(){

    // hey, this is a comment and it will be ignored by compiler
    printf("Hello world!\n");
    printf("I like pizza, it's really good!");
    return 0;
    /* if it return 1, it means that the codes have some error */
    /* 
        escape sequence = character combination consisting of a backslash \
                            followed by a letter or combination of digits.
                            They specify actions within a line or string of text.
                            \n = newline
                            \t = tab
                            \ = is used to scape some string
    
    */
}