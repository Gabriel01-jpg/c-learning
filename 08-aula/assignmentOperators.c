#include <stdio.h>

int main(){
    // augmented assignment operators = used to replace a statement where an operato
    // take a variable as one of its arguments
    // and then assings the result back to the same variable


    int x = 10;


    x = x + 2;
    x+= 2;

}